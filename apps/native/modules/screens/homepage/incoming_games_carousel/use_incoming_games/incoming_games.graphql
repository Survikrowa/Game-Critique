query IncomingGames {
    upcomingGames(limit: 20) {
        id
        name
        platforms {
            id
            name
        }
        coverUrl
        backgroundUrl
        releaseDate
    }
}