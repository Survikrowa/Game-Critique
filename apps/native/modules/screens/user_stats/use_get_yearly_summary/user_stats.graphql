query YearlySummary($year: Int!) {
  yearlySummary(year: $year) {
    totalGames
    totalHours
    averageScore
    completedThisYear
    backlogAddedThisYear
    yearlyGames
    yearlyHours
    yearlyAverageScore
  }
}